# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MovieDB_iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MovieDB_iOS
pod 'SwiftyJSON', '~> 4.0'

  target 'MovieDB_iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MovieDB_iOSUITests' do
    # Pods for testing
  end

pod 'Alamofire', '~> 5.2'
pod 'SDWebImageSwiftUI'


end
